{
  "name": "acwaverse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && node src/tests/run-node-tests.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
